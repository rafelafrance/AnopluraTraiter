Find the locations where the specimens were collected.
This is the geographic location where the host was found.
For each location found, return an object with these exact fields:
    "species": species name (string or null),
    "sex": sex of the specimen (string or null),
    "geographic_location": location where the host was found (string or null),
Return a JSON array of objects.
